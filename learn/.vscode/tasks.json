{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc -o c/a.out c/*.c",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}