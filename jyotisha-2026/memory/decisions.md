# Decisions

- Keep the repo lightweight.
- Use `lab/` as both local browsing surface and Netlify publish root.
- Use `uv` discipline for agent work.
- Use one brief per exploration under `explorations/`.
